language: java
script: mvn clean verify
before_install:
  - chmod +x mvnw
services:
  - postgresql
before_script:
  - psql -c 'create database testsystem;' -U postgres
os:
  - linux
env:
  global:
    - PGUSER=postgres
    - PGPASSWORD=super123
    - PGPORT=5432